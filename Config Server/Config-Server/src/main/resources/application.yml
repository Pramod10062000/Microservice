server:
  port: 8085

spring:
  application:
    name: Config-Server   
  
  cloud:
    config:
      server:
        git:
          uri:  https://github.com/Pramod10062000/microservice-configurations
          clone-on-start: true 